{% extends 'default.html.twig' %}

{% block main %}
    <div class="row justify-content-center">
        <div class="col-12">
            <h1 class="mb-3">Create a new user</h1>
            <div class="row">
                <div class="col-4">
                    {{ form(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}